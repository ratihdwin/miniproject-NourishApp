import 'package:flutter/material.dart';

class StuntingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mengenal Stunting'),
      ),
      body: Center(
        child: Text('Halaman Mengenal Stunting'),
      ),
    );
  }
}

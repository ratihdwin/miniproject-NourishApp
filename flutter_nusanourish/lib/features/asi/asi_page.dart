import 'package:flutter/material.dart';

class AsiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ASI Eksklusif'),
      ),
      body: Center(
        child: Text('Halaman ASI Eksklusif'),
      ),
    );
  }
}
